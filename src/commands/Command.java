package commands;

/**
 * Class that represents a command that a user can execute on the movies platform
 */
public abstract class Command {
    /**
     * Execute the command
     */
    public abstract void execute();
}
